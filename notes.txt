on cloud:
during selen working in the cloud. need to have gui ruitinely queue
for instance and when they are all finished then send chatgpt batch
then have it que the batch and wait until done

then have hash done in cloud, have it queue the instances again until done
finally when finished database should have final_tag values